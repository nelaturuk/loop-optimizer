pragma solidity >=0.5.0;
/**
 * @notice total <= __verifier_sum_uint(tokens)
 */
contract Test {
    uint a;
    int d;

    function f(uint b) public {
        uint c;
        int g;

        a = b - 1; 
        a = c + 1;

        d = d - 1;
        d = d + 1;

        d = g + 1;
    }
 }
